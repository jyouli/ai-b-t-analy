---
description: src/utils 仅放跨业务复用的通用工具；单业务专用逻辑就近存放
alwaysApply: true
---

# `src/utils` 使用范围

- **`src/utils` 视为全项目公用工具库**：只应包含与具体业务模块无强绑定、可在多处复用的纯工具（格式化、通用请求封装、与领域无关的算法等）。
- **若某函数/常量仅服务于单一业务或单一功能域**（例如某个列表页的请求体拼装、某个业务线的表单转换、仅一个上传组件使用的 MD5 计算），**不要放进 `utils`**。应放在最接近使用处的模块内，例如：
  - 与路由页面强相关：放在对应 `routes/<feature>/` 下（可与页面同目录或同文件夹内的 `*.js` 模块）；
  - 与某个组件强相关：放在该组件文件内或组件同级目录；
  - 与某个 model/service 强相关：放在对应 `models/` 或 `services/` 旁或内部。
- **新增工具前先问**：是否已有非业务耦合的复用场景？若只有当前一个调用方，优先就近存放，避免 `utils` 膨胀为「杂项业务代码堆」。
